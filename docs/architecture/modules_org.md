# modules/org Module

## Purpose
This module is responsible for the modules/org layer of the application. It encapsulates logic, components, and services specific to this domain.

## File-by-file breakdown
| File | Description |
|------|-------------|
| `modules/org/components/MineAdjacencyField.tsx` | React component for MineAdjacencyField. |
| `modules/org/components/TransferUserDialog.tsx` | React component for TransferUserDialog. |
| `modules/org/components/UsersView.tsx` | React component for UsersView. |

## Key dependencies
**Internal Modules:**
- `shared`

**External Packages:**
- `react`

## Entry points
- No explicit entry points (Internal module).
